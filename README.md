# Курск "Алгоритмы и алгоритмические языки"

В репозитории представлены решения задач практикума на C и предоставленные материалы для ознакомления.

Также можно расширит ознакомление с курсом на [teach-in](https://teach-in.ru/course/algorithms/lecture).

Основной курс и его материалы находится по адресу [https://moodle3.cs.msu.ru](https://moodle3.cs.msu.ru).

# Список задач

## 3. Задачи на работу с вещественными числами

### 3.1. Первая цифра дробной части вещественного числа
**Описание задачи:** Присвоить целой переменной `d` первую цифру из дробной части положительного вещественного числа `x`.  
Пример: если `x = 32.597`, то `d = 5`.

### 3.2. Расстояние между точками на плоскости
**Описание задачи:** Вычислить расстояние между точками с координатами `(x1, y1)` и `(x2, y2)`.  
Координаты ввести с клавиатуры попарно и присвоить переменным вещественного типа. Вывести результат на экран в виде числа с двумя знаками после запятой.

## 4. Задачи на работу с целыми числами

### 4.1. Вывод в обратном порядке цифр трехзначного числа
**Описание задачи:** Дано целое число `N`. Если `N` трехзначное, то вывести в обратном порядке и в столбик цифры, составляющие `N`.  
Для чисел с другим количеством знаков вывести текстовое сообщение: "Число не трехзначное!"

### 4.2. Определить попадание точки в заданную область на плоскости
**Описание задачи:** Точку задать парой координат `x`, `y` вещественного типа. Проверить попадание в указанную область на плоскости (см. вложенный рисунок).

## 5. Задачи на работу с арифметическими операциями

### 5.1. Простейший калькулятор
**Описание задачи:** С клавиатуры вводятся два числа и знак операции между ними "+", "-", " * " или "/" (порядок ввода - произвольный).  
Выполнить над числами соответствующее действие и напечатать результат.  
**Указание:** использовать оператор варианта.

### 5.2. Печать заданного числа римскими цифрами (автотест)
**Описание задачи:** Написать программу на Паскале. Ввести целое `k`, для `1≤k≤39` вывести значение `k` римскими цифрами, используя заглавные буквы 'I', 'V' и 'X' для римских цифр 1, 5 и 10, соответственно.  
Если значение `k` лежит вне указанного диапазона, то вывести `0`.  
**Автотест:** Полученную программу можно проверить в системе автоматической проверки - задача 7.23.

### 5.3. Количество дней в месяце
**Описание задачи:** Даны описания:  
`type month = ( jan, feb, mar, apr, may, jun, jul, aug, sep, oct, nov, dec );`  
`var d: 28..31;   m: month;`  
Переменной `d` присвоить количество дней в месяце `m` (год считать невисокосным).  
**Указание:** Ввести с клавиатуры номер месяца и вычислить соответствующее ему правильное значение `m`; далее, используя оператор варианта с переключателем типа `month`, вычислить значение `d` и вывести его на печать.

## 6. Задачи на работу с числами и циклами

### 6.1. Отбор двузначных чисел по сумме цифр
**Описание задачи:** Из чисел от 10 до 99 вывести те, сумма цифр которых равна `s` (`0 < s ≤ 18`).  
**Указание:** Использовать цикл с параметром.

### 6.2. Поиск числа Фибоначчи, большего m>1 (автотест)
**Описание задачи:** Числа Фибоначчи `f(n)` определяются формулами:  
`f(0)=f(1)=1;  f(n)=f(n-1)+f(n-2)`  при `n=2,3,...`  
Ввести целое число `m > 1` и найти первое число Фибоначчи, большее `m`.  
**Указания к решению:** Использовать цикл с предусловием `while`.  
**Автотест:** Можно протестировать программу в автоматической системе проверки - задача 5.21б.

### 6.3. Наибольший общий делитель
**Описание задачи:** Ввести два натуральных числа `2 < a, b < 10000` и вычислить их наибольший общий делитель.  
**Указания к решению:** Использовать цикл с постусловием `repeat`.

## 7. Задачи на вычисление значений функций

### 7.1. Вычисление функции косинус в заданном диапазоне значений аргумента
**Описание задачи:** Получить и использовать рекуррентную формулу для последовательного вычисления членов ряда. Использовать ряд Тейлора.

### 7.2. Вычисление произведения сложных множителей
**Описание задачи:** Вычислить:  
\[ s = \prod_{i=1}^{n} \left( 1 + \sum_{j=a_i}^{b_i} \frac{1}{j} \right) \]  
Во внешнем цикле вычислять произведение, во внутреннем – суммы.  
**Указания к решению:**
- Ввести с клавиатуры номер множителя, который нужно пропустить.
- Использовать вложенные циклы для расчета.
- Пропуск множителя осуществлять с помощью инструкции `continue`.
- Номер скобки соответствует порядковому номеру блока вычислений.

### 7.3. Вывод простых чисел из заданного диапазона
**Описание задачи:** Дано целое `n > 2`. Напечатать все простые числа из диапазона `[2, n]`.  
**Указания к решению:**
- При неправильном вводе `n` завершить программу с помощью перехода в конец программы оператором `Goto`.
- Вычисления организовать с помощью вложенных циклов – во внешнем перебрать все числа от `2` до `n`, а во внутреннем – выполнить проверку числа на принадлежность к множеству простых чисел.  
  **Замечание:** В качестве альтернативы допускается применение других алгоритмов, например, "решета Эратосфена".

## 8. Задачи на работу с массивами

### 8.1. Массив номеров нулевых элементов
**Описание задачи:** В целочисленной последовательности длиной `0 < N ≤ 100` предполагается наличие нулевых элементов. Создать и вывести на экран массив, состоящий из номеров этих элементов.  
**Указания к решению:**
- Задать количество вводимых значений `N`. Сообщить об ошибке и закончить выполнение программы, если `N` выходит за границы заданного диапазона.
- Ввести `N` значений исходной последовательности и записать их в массив.
- Хранение номеров нулевых элементов исходной последовательности организовать во вспомогательном массиве.
- После заполнения вспомогательного массива и подсчета количества нулевых элементов `M ≤ N` вывести на экран значения `N`, `M` и номера нулевых элементов.
- Если `M = 0`, вывести сообщение 'Нулевых элементов нет!'.

## 9. Задачи на работу с матрицами

### 9.1. Выполнить действия над матрицей случайных чисел
**Описание задачи:** Создать матрицу случайных целых чисел `B: array [1..5, 1..5] of integer`. Вывести матрицу `B`, а также следующие величины на экран:
- (a) Значение наибольшего элемента и его номер `(i, j)`.
- (b) Минимальное значение среди элементов матрицы, расположенных ниже главной диагонали, и его номер.
- (c) Измененную матрицу, в которой переставлены местами строки `1` и `5`.

### 9.2. Студенты, владеющие иностранными языками
**Описание задачи:** В студенческой группе 30 человек, имеющих порядковые номера от `1` до `30`. `20` из них знают английский, `15` знают немецкий, `10` знают французский. Вывести порядковые номера студентов, которые знают все три языка, а затем номера тех, которые не знают ни одного.  
**Указание:** Задачу решить с использованием типа данных множество. Случайным образом создать множества студентов, знающих языки (`20`, `15` и `10` порядковых номеров), а затем выполнить над множествами необходимые операции.

## 10. Задачи на работу со строками

### 10.1. Подсчет количества вхождений заданных символов в строку
**Описание задачи:** Используя строковую константу `v` и вводимую с экрана строку `s` длиной не более `20`:  
`const v = 'aeiou';`  
`type stroka = string [20];`  
`var s: stroka;  k: 0..20;`  
Определить и вывести на экран общее количество `(k)` вхождений в строку `s` символов, перечисленных в константе `v`.

### 10.2. Последовательность слов
**Описание задачи:** Ввести последовательность, содержащую от `1` до `n` слов (`n=10`), в каждом из которых от `1` до `k` малых латинских букв (`k=5`); между соседними словами – запятая, за последним словом – точка.  
Вывести на экран:
- (a) эту же последовательность слов, но в обратном порядке;
- (b) все слова в алфавитном порядке.

## 11. Задачи на работу с геометрическими объектами

### 11.3. Длины сторон треугольника
**Описание задачи:** Даны координаты трех вершин треугольника. Найти длины всех его сторон.  
**Указание к решению:** Вычисление длины каждой стороны выполнить с помощью функции, которая по координатам двух точек вычисляет искомую длину и возвращает значение типа `real`.

### 11.4. Корни двух квадратных уравнений (задача 11.22б из тестирующей системы)
**Описание задачи:** Написать программу на Паскале. Даны 2 вещественных числа `a` и `b`. Если оба уравнения `x^2+6.2x+a^2=0` и `x^2+ax+b-1=0` имеют вещественные корни, то вывести логическое значение `true`; затем, вывести снова `true`, если оба корня первого уравнения лежат между корнями второго уравнения, и `false` иначе. Если же хотя бы одно из двух приведенных выше уравнений не имеет вещественных корней, вывести `false`.  
**Указание к решению:** Программа должна начинаться с описания функции  
`function Roots(p,q:real; var x1,x2:real):Boolean;`  
которая вычисляет вещественные корни приведённого квадратного уравнения `x^2+px+q=0` и присваивает результат выходным параметрам `x1` и `x2`. Значение функции - `true`, если вещественные корни существуют (дискриминант `D≥0`), и `false` иначе.

## 12. Задачи на поиск корней функций

### 12.1. Поиск нуля функции методом деления отрезка пополам: рекурсия
**Описание задачи:** Найти корень уравнения `f(x) = 0` на промежутке `[a, b]` методом деления отрезка пополам. Предусмотреть выбор минимум двух произвольных непрерывных и монотонных функций, принимающих на концах заданного отрезка значения разных знаков. Значения функций вычислять с помощью соответствующих процедур-функций.  
**Указания к решению:**  
Необходимо написать рекурсивную функцию `root` с параметрами `f`, `a`, `b` и `eps` (`f` - функциональный параметр, определяющий функцию, для которой ищется корень, `a` и `b` задают промежуток, а `eps` - точность определения корня) и вычисляющую текущее приближение для корня заданной функции `f(x)` следующим образом:
- Тривиальный случай: длина исследуемого отрезка меньше заданного значения `eps`. В этом случае любую точку отрезка можно принимать за приближенное значение корня.
- Если длина исследуемого участка более `eps`, то находится середина отрезка `c` и вычисляется значение функции `f(c)`. В зависимости от значения `f(c)` продолжается поиск корня либо в промежутке `[a, c]`, либо в `[c, b]`, а именно: если на концах промежутка `[a, c]` значения функции противоположных знаков, то поиск корня осуществляется на этом промежутке с помощью рекурсивного вызова `root(a, c, eps)`, в противном случае нахождение корня обеспечит вызов `root(c, b, eps)`.  
  С помощью функции `root` решить уравнения для выбранных функций, используя функциональный параметр.

## 13. Задачи на работу с комплексными числами

### 13.1. Операции с комплексными числами
(a) Написать функции суммы, разности, умножения и деления комплексных чисел и разместить их в отдельном модуле `complexn.pas`.  
(b) В основной программе, используя модуль, вычислить:  
\[ 0.25 \cdot \left[ \frac{(17+31i)}{(7+i)} + \frac{12}{(1+i)^4} \right] + i \]  
где значок \(^\) обозначает возведение в степень, а \(i\) - это мнимая единица.  
**Указание к решению:** использовать формулы суммы, разности, произведения и частного комплексных чисел:  
\[ (a+ib)+(c+id) = (a+c)+i(b+d) \]  
\[ (a+ib)-(c+id) = (a-c)+i(b-d) \]  
\[ (a+ib)\cdot(c+id) = (ac-bd)+i(ad+bc) \]  
\[ \frac{(a+ib)}{(c+id)} = \frac{ac+bd}{c^2+d^2}+i\frac{bc-ad}{c^2+d^2} \]

## 14. Задачи на создание файлов

### 14.2. Создание файла с данными для зачётного проекта (ДНК)
**Описание задачи:** Ознакомиться с содержанием зачётного проекта - задачей 17.3. Далее:
1. Ввести с клавиатуры целое число `1 ≤ N ≤ 1000`.
2. По таблице 1 из задачи 17.3 (зачётный проект) выбрать случайным образом `N` аминокислотных остатков (трёхбуквенные обозначения) и записать их в текстовый файл `NC_sequence.txt`. Таким образом, общее число символов в файле будет равно `3\cdot N`.
3. Закрыть файл.
4. Открыть файл и прочитать из него массив символов и подсчитать количество триплетов, соответствующих каждому аминокислотному остатку.
5. Вывести на экран построчно: количество, триплет, наименование остатка (см. Приложение 1 из задачи 17.3).

## 15. Задачи на работу с упорядоченными списками

### 15.1. Объединение двух упорядоченных списков
**Описание задачи:** Даны два упорядоченных по неубыванию списка `L1` и `L2`, в которых хранятся натуральные числа в диапазоне `[1..99]`. Требуется объединить списки, сохранив результат в `L1`.  
**Указание:**
1. Создать списки `L1` и `L2`, заполняя их случайными числами в неубывающем порядке (написать функцию вставки элемента в упорядоченный список).
2. Распечатать содержимое списков `L1` и `L2`.
3. Объединить списки, сохранив в `L1` неубывающий порядок элементов.
4. Вывести список `L1` на печать.
5. Освободить память, занятую списками.

## 17. Задачи на биоинформатику

### 17.3. Поиск и классификация мутаций в цепочке ДНК
**Описание задачи:** Есть достаточно длинный фрагмент ДНК в текстовом файле (`NC_sequence.txt`, не более 3000 символов).

1. Ввести ДНК из файла, содержащего последовательность символов (обозначающих нуклеотиды).
2. Сделать `N` мутаций случайным образом (ввести это значение с клавиатуры). Одна мутация — точечная замена одного нуклеотида.
3. По карте генетического кода (принадлежности одной из четырех групп) выяснить, изменились ли свойства гена.
4. Вывести на экран построчно для каждого измененного триплета:
    - Номер позиции триплета в последовательности.
    - Трехбуквенное обозначение триплета до мутации.
    - Номер группы, которой он принадлежит.
    - Трехбуквенное обозначение мутированного триплета.
    - Номер группы, которой он принадлежит.
    - Комментарий "Свойства остатка не изменились после мутации" или "Мутация привела к изменению свойств остатка".
